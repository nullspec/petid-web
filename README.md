# petid-web

> PetId

## Build Setup

``` bash
# install dependencies
yarn install

# serve with hot reload at localhost:8080
yarn server

# run unit tests
yarn run test:unit

# run e2e tests
yarn run test:e2e
```